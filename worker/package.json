{
  "name": "chatgpt-screenshot-gallery-vote-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply chatgpt-screenshot-gallery-votes --local",
    "db:migrate:remote": "wrangler d1 migrations apply chatgpt-screenshot-gallery-votes --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260303.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.7.1"
  }
}
